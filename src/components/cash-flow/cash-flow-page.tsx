"use client";
import { useState } from "react";
import { Plus, TrendingDown, TrendingUp, Trash2 } from "lucide-react";
import { createExpense, createIncome, deleteExpense, deleteIncome } from "@/actions/cash-flow";
import { rupiah } from "@/lib/currency";
type Tx={id:string;label:string;amount:number;date:string;notes:string;type:"income"|"expense"};
export function CashFlowPage({items}:{items:Tx[]}) { const [open,setOpen]=useState(false); const [type,setType]=useState<"income"|"expense">("income"); const income=items.filter(x=>x.type==="income").reduce((n,x)=>n+x.amount,0); const expense=items.filter(x=>x.type==="expense").reduce((n,x)=>n+x.amount,0); const action=type==="income"?createIncome:createExpense; return <div className="mx-auto max-w-6xl p-5 md:p-8"><header className="flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-semibold tracking-tight">Cash Flow</h1><p className="mt-2 text-slate-600">Track money coming in and going out.</p></div><button onClick={()=>setOpen(true)} className="rounded-xl bg-emerald-700 px-4 py-2.5 text-sm font-semibold text-white"><Plus className="mr-1 inline" size={17}/>Add Transaction</button></header><div className="mt-7 grid gap-4 sm:grid-cols-3"><Stat label="Income" value={income} tone="green"/><Stat label="Expenses" value={expense} tone="red"/><Stat label="Net Cash Flow" value={income-expense} tone={income-expense>=0?"green":"red"}/></div><section className="mt-6 rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b p-5"><div><h2 className="text-lg font-semibold">Transactions</h2><p className="text-sm text-slate-500">Recent income and expenses</p></div></div>{items.length?items.map(tx=><div key={tx.type+tx.id} className="flex items-center justify-between border-b px-5 py-4 last:border-0"><div className="flex gap-3"><span className={`grid size-9 place-items-center rounded-full ${tx.type==="income"?"bg-emerald-50 text-emerald-700":"bg-rose-50 text-rose-700"}`}>{tx.type==="income"?<TrendingUp size={18}/>:<TrendingDown size={18}/>}</span><div><strong className="text-sm">{tx.label}</strong><p className="text-xs text-slate-500">{tx.type==="income"?"Income":"Expense"} · {tx.date}{tx.notes?` · ${tx.notes}`:""}</p></div></div><div className="flex items-center gap-3"><strong className={tx.type==="income"?"text-emerald-700":"text-rose-700"}>{tx.type==="income"?"+ ":"- "}{rupiah(tx.amount)}</strong><form action={tx.type==="income"?deleteIncome:deleteExpense}><input type="hidden" name="id" value={tx.id}/><button className="text-slate-400 hover:text-rose-600"><Trash2 size={16}/></button></form></div></div>):<p className="p-10 text-center text-sm text-slate-500">No transactions yet. Add income or expense to start tracking cash flow.</p>}</section>{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/30 p-4"><form action={async f=>{await action(f);setOpen(false)}} className="w-full max-w-md rounded-2xl bg-white p-6 shadow-xl"><h2 className="text-lg font-semibold">Add Transaction</h2><div className="mt-4 flex rounded-lg bg-slate-100 p-1"><button type="button" onClick={()=>setType("income")} className={`flex-1 rounded-md py-2 text-sm ${type==="income"?"bg-white font-semibold shadow":""}`}>Income</button><button type="button" onClick={()=>setType("expense")} className={`flex-1 rounded-md py-2 text-sm ${type==="expense"?"bg-white font-semibold shadow":""}`}>Expense</button></div><label className="mt-4 block text-sm">{type==="income"?"Source":"Description"}<input name="label" required className="mt-1 w-full rounded-xl border p-2.5"/></label><label className="mt-3 block text-sm">Amount<input name="amount" type="number" min="1" required className="mt-1 w-full rounded-xl border p-2.5"/></label><label className="mt-3 block text-sm">Date<input name="date" type="date" required className="mt-1 w-full rounded-xl border p-2.5"/></label><label className="mt-3 block text-sm">{type==="income"?"Notes":"Category"}<input name="notes" className="mt-1 w-full rounded-xl border p-2.5"/></label><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={()=>setOpen(false)} className="px-3 py-2 text-sm">Cancel</button><button className="rounded-xl bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Save {type==="income"?"Income":"Expense"}</button></div></form></div>}</div>; }
function Stat({label,value,tone}:{label:string;value:number;tone:"green"|"red"}) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><strong className={`mt-2 block text-2xl ${tone==="green"?"text-emerald-700":"text-rose-700"}`}>{rupiah(value)}</strong></section>; }
