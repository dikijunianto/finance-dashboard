"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { BarChart3, CalendarDays, CircleDollarSign, CreditCard, FileText, LayoutDashboard, Menu, Settings, Target, Wallet } from "lucide-react";
import { logout } from "@/actions/auth";
const links = [{ href: "/dashboard", label: "Dashboard", icon: LayoutDashboard }, { href: "/cash-flow", label: "Cash Flow", icon: BarChart3 }, { href: "/bills", label: "Bills & Debt", icon: CreditCard }, { href: "/budget", label: "Budget", icon: Wallet }, { href: "/goals", label: "Goals", icon: Target }, { href: "/payday", label: "Payday Plan", icon: CalendarDays }, { href: "/reports", label: "Reports", icon: FileText }, { href: "/settings", label: "CFO Notes", icon: Settings }];
export function DashboardShell({ children }: { children: React.ReactNode }) { const path = usePathname(); return <div className="min-h-screen lg:grid lg:grid-cols-[250px_1fr]"><aside className="hidden border-r border-[var(--line)] bg-white p-5 lg:block"><div className="mb-10 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-emerald-700 text-white"><CircleDollarSign size={22}/></span><div><strong>MyFinance</strong><p className="text-xs text-[var(--muted)]">Your Personal CFO</p></div></div><nav className="space-y-1">{links.map(({ href, label, icon: Icon }) => <Link key={href} href={href} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium ${path === href ? "bg-emerald-50 text-emerald-800" : "text-slate-600 hover:bg-slate-50"}`}><Icon size={18}/>{label}</Link>)}</nav><div className="mt-10 rounded-xl bg-slate-50 p-3 text-sm"><strong>Diki</strong><p className="text-xs text-slate-500">Private account</p><form action={logout}><button className="mt-3 text-xs font-semibold text-rose-700">Logout</button></form></div></aside><main><header className="flex items-center justify-between border-b border-[var(--line)] bg-white px-5 py-4 lg:hidden"><div className="flex items-center gap-2 font-bold"><Menu size={20}/> MyFinance</div><form action={logout}><button className="text-sm font-semibold text-rose-700">Logout</button></form></header>{children}</main></div>; }
