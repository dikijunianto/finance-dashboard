"use client";
import { useState } from "react";
import { Plus, Trash2 } from "lucide-react";
import { rupiah } from "@/lib/currency";
type Item = { id: number; name: string; detail: string; amount: number };
export function WorkspacePage({ title, subtitle, initial }: { title: string; subtitle: string; initial: Item[] }) { const [items, setItems] = useState(initial); const [name, setName] = useState(""); const [amount, setAmount] = useState(""); const add = () => { if (!name || !Number(amount)) return; setItems([{ id: Date.now(), name, amount: Number(amount), detail: "Baru ditambahkan" }, ...items]); setName(""); setAmount(""); }; return <div className="mx-auto max-w-5xl p-5 md:p-8"><header className="mb-8"><h1 className="text-3xl font-bold">{title}</h1><p className="mt-1 text-slate-600">{subtitle}</p></header><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex flex-col gap-3 sm:flex-row"><input value={name} onChange={e => setName(e.target.value)} placeholder="Nama" className="rounded-xl border border-slate-200 px-3 py-2"/><input value={amount} onChange={e => setAmount(e.target.value)} placeholder="Nominal Rupiah" type="number" className="rounded-xl border border-slate-200 px-3 py-2"/><button onClick={add} className="inline-flex items-center justify-center gap-2 rounded-xl bg-emerald-700 px-4 py-2 font-semibold text-white"><Plus size={17}/> Tambah</button></div><div className="mt-5 divide-y">{items.length ? items.map(item => <div className="flex items-center justify-between py-4" key={item.id}><div><strong>{item.name}</strong><p className="text-sm text-slate-500">{item.detail}</p></div><div className="flex items-center gap-4"><strong>{rupiah(item.amount)}</strong><button aria-label={`Hapus ${item.name}`} onClick={() => setItems(items.filter(x => x.id !== item.id))} className="text-rose-600"><Trash2 size={18}/></button></div></div>) : <p className="py-10 text-center text-slate-500">Belum ada data.</p>}</div></section></div>; }
