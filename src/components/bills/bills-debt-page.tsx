"use client";
import { useState } from "react";
import { createBill, createDebt, deleteBill, deleteDebt, markBillPaid, recordDebtPayment } from "@/actions/bills-debt";
import { Dialog, DialogClose, DialogContent, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { rupiah } from "@/lib/currency";
type Bill={id:string;name:string;amount:number;dueDay:number;notes:string|null};type Debt={id:string;name:string;remainingAmount:number;originalAmount:number;installmentAmount:number;dueDay:number};
export function BillsDebtPage({bills,debts}:{bills:Bill[];debts:Debt[]}){const [tab,setTab]=useState<"bills"|"debt">("bills");const total=debts.reduce((n,d)=>n+d.remainingAmount,0);return <div className="mx-auto max-w-6xl p-5 md:p-8"><h1 className="text-3xl font-semibold">Bills & Debt</h1><p className="mt-2 text-slate-600">Stay ahead of upcoming obligations.</p><div className="mt-6 flex gap-2 border-b"><button onClick={()=>setTab("bills")} className={`px-4 py-3 text-sm ${tab==="bills"?"border-b-2 border-emerald-700 font-semibold text-emerald-700":"text-slate-500"}`}>Bills</button><button onClick={()=>setTab("debt")} className={`px-4 py-3 text-sm ${tab==="debt"?"border-b-2 border-emerald-700 font-semibold text-emerald-700":"text-slate-500"}`}>Debt</button><EntryDialog type={tab}/></div>{tab==="bills"?<section className="mt-6 grid gap-4 md:grid-cols-3"><Stat label="Due This Month" value={bills.reduce((n,b)=>n+b.amount,0)}/><Stat label="Paid This Month" value={0}/><Stat label="Remaining" value={bills.reduce((n,b)=>n+b.amount,0)}/><div className="md:col-span-3 rounded-2xl border bg-white">{bills.length?bills.map(b=><div key={b.id} className="flex justify-between border-b p-5"><div><strong>{b.name}</strong><p className="text-sm text-slate-500">Due day {b.dueDay} · Monthly</p></div><div className="text-right"><strong>{rupiah(b.amount)}</strong><div className="mt-2 flex gap-2"><form action={markBillPaid}><input name="id" type="hidden" value={b.id}/><button className="text-sm text-emerald-700">Mark Paid</button></form><form action={deleteBill}><input name="id" type="hidden" value={b.id}/><button className="text-sm text-rose-600">Delete</button></form></div></div></div>):<p className="p-10 text-center text-sm text-slate-500">No bills yet. Add recurring bills to track upcoming obligations.</p>}</div></section>:<section className="mt-6"><div className="grid gap-4 md:grid-cols-3"><Stat label="Total Remaining Debt" value={total}/><Stat label="Monthly Payments" value={debts.reduce((n,d)=>n+d.installmentAmount,0)}/><Stat label="Active Debts" value={debts.length}/></div><div className="mt-6 grid gap-4 md:grid-cols-2">{debts.map(d=>{const p=d.originalAmount?Math.round((1-d.remainingAmount/d.originalAmount)*100):0;return <article key={d.id} className="rounded-2xl border bg-white p-5"><strong>{d.name}</strong><p className="mt-4 text-sm text-slate-500">Remaining</p><b className="text-2xl">{rupiah(d.remainingAmount)}</b><p className="mt-2 text-sm">{p}% paid · {rupiah(d.installmentAmount)}/month · Due {d.dueDay}</p><div className="mt-3 h-2 rounded bg-slate-100"><div className="h-full rounded bg-emerald-600" style={{width:`${p}%`}}/></div><PaymentDialog debt={d}/><form action={deleteDebt}><input name="id" type="hidden" value={d.id}/><button className="mt-3 text-sm text-rose-600">Delete</button></form></article>})}</div></section>}</div>}
function EntryDialog({type}:{type:"bills"|"debt"}){const [open,setOpen]=useState(false);const action=type==="bills"?createBill:createDebt;return <Dialog open={open} onOpenChange={setOpen}><button onClick={()=>setOpen(true)} className="ml-auto mb-2 rounded-xl bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">+ Add {type==="bills"?"Bill":"Debt"}</button><DialogContent><DialogHeader><DialogTitle className="text-lg font-semibold">Add {type==="bills"?"Bill":"Debt"}</DialogTitle></DialogHeader><form key={open?"open":"closed"} action={async f=>{await action(f);setOpen(false)}}><input name="name" placeholder={type==="bills"?"Bill name":"Debt name"} className="w-full rounded-lg border p-2" required/>{type==="bills"?<><input name="amount" type="number" placeholder="Amount" className="mt-3 w-full rounded-lg border p-2" required/><input name="dueDay" type="number" placeholder="Due day" className="mt-3 w-full rounded-lg border p-2" required/><input name="category" placeholder="Category" className="mt-3 w-full rounded-lg border p-2" required/><input name="notes" placeholder="Notes" className="mt-3 w-full rounded-lg border p-2"/></>:<><input name="lender" placeholder="Lender" className="mt-3 w-full rounded-lg border p-2" required/><input name="originalAmount" type="number" placeholder="Original amount" className="mt-3 w-full rounded-lg border p-2" required/><input name="remainingAmount" type="number" placeholder="Remaining amount" className="mt-3 w-full rounded-lg border p-2" required/><input name="installmentAmount" type="number" placeholder="Monthly payment" className="mt-3 w-full rounded-lg border p-2" required/><input name="dueDay" type="number" placeholder="Due day" className="mt-3 w-full rounded-lg border p-2" required/></>}<DialogFooter><DialogClose type="button" className="rounded-xl border px-4 py-2 text-sm">Cancel</DialogClose><button className="rounded-xl bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Save {type==="bills"?"Bill":"Debt"}</button></DialogFooter></form></DialogContent></Dialog>}
function PaymentDialog({debt}:{debt:Debt}){const [open,setOpen]=useState(false);return <Dialog open={open} onOpenChange={setOpen}><button onClick={()=>setOpen(true)} className="mt-4 text-sm text-emerald-700">Record Payment</button><DialogContent><DialogHeader><DialogTitle className="text-lg font-semibold">Record Payment</DialogTitle><p className="text-sm text-slate-500">{debt.name} · Remaining {rupiah(debt.remainingAmount)}</p></DialogHeader><form key={open?"open":"closed"} action={async f=>{await recordDebtPayment(f);setOpen(false)}}><input name="id" type="hidden" value={debt.id}/><input name="amount" type="number" min="1" placeholder="Payment amount" className="w-full rounded-lg border p-2" required/><DialogFooter><DialogClose type="button" className="rounded-xl border px-4 py-2 text-sm">Cancel</DialogClose><button className="rounded-xl bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">Record Payment</button></DialogFooter></form></DialogContent></Dialog>}
function Stat({label,value}:{label:string;value:number}){return <div className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{label}</p><strong className="mt-2 block text-2xl">{label==="Active Debts"?value:rupiah(value)}</strong></div>}
