"use client";
import { useState } from "react";
import { createBill, createDebt, deleteBill, deleteDebt, markBillPaid, recordDebtPayment } from "@/actions/bills-debt";
import { rupiah } from "@/lib/currency";
type Bill={id:string;name:string;amount:number;dueDay:number;notes:string|null};type Debt={id:string;name:string;remainingAmount:number;originalAmount:number;installmentAmount:number;dueDay:number};
export function BillsDebtPage({bills,debts}:{bills:Bill[];debts:Debt[]}){const [tab,setTab]=useState<"bills"|"debt">("bills");const [open,setOpen]=useState(false);const total=debts.reduce((n,d)=>n+d.remainingAmount,0);return <div className="mx-auto max-w-6xl p-5 md:p-8"><h1 className="text-3xl font-semibold">Bills & Debt</h1><p className="mt-2 text-slate-600">Stay ahead of upcoming obligations.</p><div className="mt-6 flex gap-2 border-b"><button onClick={()=>setTab("bills")} className={`px-4 py-3 text-sm ${tab==="bills"?"border-b-2 border-emerald-700 font-semibold text-emerald-700":"text-slate-500"}`}>Bills</button><button onClick={()=>setTab("debt")} className={`px-4 py-3 text-sm ${tab==="debt"?"border-b-2 border-emerald-700 font-semibold text-emerald-700":"text-slate-500"}`}>Debt</button><button onClick={()=>setOpen(true)} className="ml-auto mb-2 rounded-xl bg-emerald-700 px-4 py-2 text-sm font-semibold text-white">+ Add {tab==="bills"?"Bill":"Debt"}</button></div>{tab==="bills"?<section className="mt-6 grid gap-4 md:grid-cols-3"><Stat label="Due This Month" value={bills.reduce((n,b)=>n+b.amount,0)}/><Stat label="Paid This Month" value={0}/><Stat label="Remaining" value={bills.reduce((n,b)=>n+b.amount,0)}/><div className="md:col-span-3 rounded-2xl border bg-white">{bills.length?bills.map(b=><div key={b.id} className="flex justify-between border-b p-5"><div><strong>{b.name}</strong><p className="text-sm text-slate-500">Due day {b.dueDay} · Monthly</p></div><div className="text-right"><strong>{rupiah(b.amount)}</strong><div className="mt-2 flex gap-2"><form action={markBillPaid}><input name="id" type="hidden" value={b.id}/><button className="text-sm text-emerald-700">Mark Paid</button></form><form action={deleteBill}><input name="id" type="hidden" value={b.id}/><button className="text-sm text-rose-600">Delete</button></form></div></div></div>):<p className="p-10 text-center text-sm text-slate-500">No bills yet. Add recurring bills to track upcoming obligations.</p>}</div></section>:<section className="mt-6"><div className="grid gap-4 md:grid-cols-3"><Stat label="Total Remaining Debt" value={total}/><Stat label="Monthly Payments" value={debts.reduce((n,d)=>n+d.installmentAmount,0)}/><Stat label="Active Debts" value={debts.length}/></div><div className="mt-6 grid gap-4 md:grid-cols-2">{debts.map(d=>{const p=d.originalAmount?Math.round((1-d.remainingAmount/d.originalAmount)*100):0;return <article key={d.id} className="rounded-2xl border bg-white p-5"><strong>{d.name}</strong><p className="mt-4 text-sm text-slate-500">Remaining</p><b className="text-2xl">{rupiah(d.remainingAmount)}</b><p className="mt-2 text-sm">{p}% paid · {rupiah(d.installmentAmount)}/month · Due {d.dueDay}</p><div className="mt-3 h-2 rounded bg-slate-100"><div className="h-full rounded bg-emerald-600" style={{width:`${p}%`}}/></div><form action={recordDebtPayment} className="mt-4 flex gap-2"><input name="id" type="hidden" value={d.id}/><input name="amount" type="number" min="1" placeholder="Payment" className="w-full rounded-lg border px-2"/><button className="text-sm text-emerald-700">Record Payment</button></form><form action={deleteDebt}><input name="id" type="hidden" value={d.id}/><button className="mt-3 text-sm text-rose-600">Delete</button></form></article>})}</div></section>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-black/30 p-4"><form action={async f=>{await(tab==="bills"?createBill:createDebt)(f);setOpen(false)}} className="w-full max-w-md rounded-2xl bg-white p-6"><h2 className="text-lg font-semibold">Add {tab==="bills"?"Bill":"Debt"}</h2><input name="name" placeholder={tab==="bills"?"Bill name":"Debt name"} className="mt-4 w-full rounded-lg border p-2" required/><input name="amount" placeholder="Amount" type="number" className={`mt-3 w-full rounded-lg border p-2 ${tab==="debt"?"hidden":""}`} required={tab==="bills"}/>{tab==="bills"?<><input name="dueDay" type="number" placeholder="Due day" className="mt-3 w-full rounded-lg border p-2" required/><input name="category" placeholder="Category" className="mt-3 w-full rounded-lg border p-2" required/><input name="notes" placeholder="Notes" className="mt-3 w-full rounded-lg border p-2"/></>:<><input name="lender" placeholder="Lender" className="mt-3 w-full rounded-lg border p-2" required/><input name="originalAmount" type="number" placeholder="Original amount" className="mt-3 w-full rounded-lg border p-2" required/><input name="remainingAmount" type="number" placeholder="Remaining amount" className="mt-3 w-full rounded-lg border p-2" required/><input name="installmentAmount" type="number" placeholder="Monthly payment" className="mt-3 w-full rounded-lg border p-2" required/><input name="dueDay" type="number" placeholder="Due day" className="mt-3 w-full rounded-lg border p-2" required/></>}<button className="mt-5 rounded-xl bg-emerald-700 px-4 py-2 text-white">Save</button></form></div>}</div>}
function Stat({label,value}:{label:string;value:number}){return <div className="rounded-2xl border bg-white p-5"><p className="text-sm text-slate-500">{label}</p><strong className="mt-2 block text-2xl">{typeof value==="number"&&label==="Active Debts"?value:rupiah(value)}</strong></div>}
