import { getDashboardData } from "@/lib/dashboard/data";
import { rupiah } from "@/lib/currency";
export default async function Page() { const data=await getDashboardData(); const rows=[["Income",data.cashFlow[0]?.income??0],["Expenses",data.cashFlow[0]?.expenses??0],["Monthly surplus",data.surplus],["Total debt",data.debtTotal],["Safe-to-spend",data.safe]]; return <div className="mx-auto max-w-6xl p-5 md:p-8"><p className="text-sm font-semibold text-emerald-700">Monthly review</p><h1 className="mt-1 text-3xl font-bold">Reports</h1><p className="mt-2 text-slate-600">A calm, read-only summary for your next financial decision.</p><section className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{rows.map(([label,value])=><div key={String(label)} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><strong className="mt-2 block text-2xl">{rupiah(Number(value))}</strong></div>)}</section><section className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold">CFO Notes</h2>{data.notes.length?<ul className="mt-4 space-y-2 text-sm">{data.notes.map(n=><li key={n.id}>• {n.content}</li>)}</ul>:<p className="mt-3 text-sm text-slate-500">No notes for this period.</p>}</section></div>; }
